<?xml version="1.0" encoding="utf-8"?>
<!-- activity_settings.xml -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Header with App Name and User Profile Picture -->
    <!-- Use appropriate views or widgets for displaying the app name and user profile picture -->

    <!-- Toggle for Dark Mode -->
    <ImageView
        android:id="@+id/applogo"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/logo"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"/>

    <TextView
        android:id="@+id/appName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Quick Notes"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_below="@id/applogo"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"/>
    <TextView
        android:id="@+id/darkModeTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Dark Mode"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_below="@id/appName"
        android:layout_marginTop="16dp"/>

    <Switch
        android:id="@+id/darkModeSwitch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/darkModeTextView"
        android:layout_marginTop="8dp"/>

    <!-- Other Customizable Preferences -->
    <!-- Add other settings options like font size, notification settings, etc. -->
    <!-- Adjust layout and style based on preferences -->

    <!-- Option to Log Out -->
    <Button
        android:id="@+id/logoutButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Log Out"
        android:layout_below="@id/otherSettingsOptions"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp"/>

</RelativeLayout>
